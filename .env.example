# Database connection string
# Replace with your PostgreSQL connection details
DATABASE_URL="postgresql://user:password@localhost:5432/ai_news_aggregator"

# Cron secret for automated article fetching
# Set to any secure random string in production
CRON_SECRET=dev-secret-change-in-production

# Note: OpenAI API key is NOT required in this file
# Users provide their own API keys through the application UI
# This keeps your deployment secure and allows users to control their own costs
